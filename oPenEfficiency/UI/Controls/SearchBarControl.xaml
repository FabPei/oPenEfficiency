<UserControl x:Class="oPenEfficiency.UI.SearchBarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" HorizontalAlignment="Stretch">
    <Border Margin="4,4,4,4" Background="{DynamicResource SurfaceBrush}" 
            CornerRadius="4" BorderBrush="{DynamicResource BorderLightBrush}" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <Viewbox Width="12" Height="12" Margin="6,0,4,0" Opacity="0.5">
                <Path Fill="{DynamicResource TextPrimaryBrush}" 
                      Data="M15.5,14H14.71L14.43,13.73C15.41,12.59 16,11.11 16,9.5A6.5,6.5 0 0,0 9.5,3A6.5,6.5 0 0,0 3,9.5A6.5,6.5 0 0,0 9.5,16C11.11,16 12.59,15.41 13.73,14.43L14,14.71V15.5L19,20.5L20.5,19L15.5,14M9.5,14C7,14 5,12 5,9.5C5,7 7,5 9.5,5C12,5 14,7 14,9.5C14,12 12,14 9.5,14Z"/>
            </Viewbox>

            <TextBox x:Name="TxtSearch" Grid.Column="1" 
                     Background="Transparent" BorderThickness="0"
                     Foreground="{DynamicResource TextPrimaryBrush}"
                     VerticalContentAlignment="Center" Padding="2,4"
                     FontSize="12" GotFocus="TxtSearch_GotFocus" LostFocus="TxtSearch_LostFocus"
                     TextChanged="TxtSearch_TextChanged" />
            
            <TextBlock x:Name="TxtSearchPlaceholder" Grid.Column="1" 
                       Text="Search features..." Margin="4,0,0,0"
                       Foreground="{DynamicResource TextMutedBrush}"
                       IsHitTestVisible="False" VerticalAlignment="Center"
                       Visibility="Visible" FontSize="11" FontStyle="Italic"/>
        </Grid>
    </Border>
</UserControl>