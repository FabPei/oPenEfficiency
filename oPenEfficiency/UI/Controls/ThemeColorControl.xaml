<UserControl x:Class="oPenEfficiency.UI.ThemeColorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300">
    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <Grid x:Name="RootContainer" Margin="10">
                <!-- Multi-Column Layout (Default) -->
                <StackPanel x:Name="StandardLayout" HorizontalAlignment="Center">
                    <TextBlock Text="FILL" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,0,5"/>
                    <UniformGrid x:Name="GridFill" Columns="10" Margin="0,0,0,5"/>

                    <TextBlock Text="CUSTOM" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,5,0,5"/>
                    <UniformGrid x:Name="GridCustom1" Columns="10" Margin="0,0,0,2"/>
                    <UniformGrid x:Name="GridCustom2" Columns="10" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="BORDER" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,0,5"/>
                    <UniformGrid x:Name="GridBorder" Columns="10" Margin="0,0,0,15"/>
                    
                    <TextBlock Text="TEXT" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,0,0,5"/>
                    <UniformGrid x:Name="GridText" Columns="10" Margin="0,0,0,5"/>
                </StackPanel>

                <!-- Vertical 3-Column Layout (Docked) -->
                <StackPanel x:Name="VerticalLayout" Visibility="Collapsed">
                    <UniformGrid x:Name="GridVertical" Columns="3" HorizontalAlignment="Center" Width="80"/>
                    
                    <TextBlock Text="CUSTOM" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="9" FontWeight="Bold" Margin="0,15,0,5" HorizontalAlignment="Center"/>
                    <UniformGrid x:Name="GridCustomVertical" Columns="3" Margin="0,0,0,10" HorizontalAlignment="Center" Width="80"/>
                </StackPanel>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
