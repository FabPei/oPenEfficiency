<UserControl x:Class="oPenEfficiency.UI.Tabs.ShapeLibraryTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:features="clr-namespace:oPenEfficiency.Features"
             xmlns:local="clr-namespace:oPenEfficiency.UI"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource WindowBackgroundBrush}"
             PreviewMouseWheel="UserControl_PreviewMouseWheel">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/oPenEfficiency;component/UI/Styles/Themes/Dark.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <local:BooleanToVisibilityConverter x:Key="BoolToVis"/>
            <local:InverseBooleanToVisibilityConverter x:Key="InvBoolToVis"/>

            <ContextMenu x:Key="LibraryItemContextMenu">
                <ContextMenu.Resources>
                    <Style TargetType="MenuItem">
                        <Setter Property="Padding" Value="10,5"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="MenuItem">
                                    <Border x:Name="Bd" Background="Transparent" BorderThickness="0" Padding="{TemplateBinding Padding}">
                                        <ContentPresenter ContentSource="Header" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsHighlighted" Value="True">
                                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ContextMenu.Resources>
                <MenuItem Header="Insert" Click="BtnInsertShapeContext_Click"/>
                <Separator/>
                <MenuItem Header="Add Selection to Favorites..." Click="BtnFavoriteSelectionContext_Click"/>
                <MenuItem Header="Add this to Favorites" Click="BtnAddThisToFavorites_Click" Visibility="{Binding IsFavorite, Converter={StaticResource InvBoolToVis}}"/>
                <Separator/>
                <MenuItem Header="Edit Tags..." Click="BtnEditDetails_Click"/>
                <MenuItem Header="Remove from Favorites" Click="BtnDeleteAsset_Click" Foreground="Red" Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVis}}"/>
            </ContextMenu>

            <Style x:Key="LibraryItemStyle" TargetType="ListBoxItem">
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Padding" Value="4"/>
                <Setter Property="Margin" Value="4"/>
                <Setter Property="Background" Value="{DynamicResource SurfaceLightBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderLightBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="ContextMenu" Value="{StaticResource LibraryItemContextMenu}"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ListBoxItem">
                            <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="true">
                                    <Setter Property="Background" TargetName="Bd" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource AccentHoverBrush}"/>
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="true">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceHoverBrush}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border x:Name="BrowserToolbar" Grid.Row="0" Background="{DynamicResource SurfaceBrush}" Padding="12,8" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBox x:Name="TxtSearch" Width="150" Margin="0,0,10,0" TextChanged="TxtSearch_TextChanged"/>
                <Button Content="Refresh" Click="BtnRefresh_Click" Padding="8,4"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid x:Name="BrowserView">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" MinWidth="120" MaxWidth="300"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*" MinWidth="0"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="{DynamicResource WindowBackgroundBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,1,0">
                    <DockPanel>
                        <Border DockPanel.Dock="Top" Background="{DynamicResource SurfaceHoverBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,0,0,1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border x:Name="BtnShowAll" Padding="10,6" Cursor="Hand" MouseLeftButtonUp="BtnShowAll_Click">
                                    <TextBlock Text="🔷 All Objects" FontSize="11" Foreground="{DynamicResource AccentHoverBrush}" FontWeight="SemiBold"/>
                                </Border>
                                <Button Grid.Column="1" Content="📁+" ToolTip="Create New Folder" Click="BtnNewFolder_Click" 
                                        Background="Transparent" BorderThickness="0" Foreground="{DynamicResource TextPrimaryBrush}" 
                                        Padding="8,4" Cursor="Hand" VerticalAlignment="Center" Margin="0,0,5,0"/>
                            </Grid>
                        </Border>
                        <TreeView x:Name="FolderTree" BorderThickness="0" Background="Transparent" SelectedItemChanged="FolderTree_SelectedItemChanged">
                            <TreeView.Resources>
                                <ContextMenu x:Key="FolderTreeContextMenu">
                                    <MenuItem Header="Refresh Cache" Click="BtnRefreshCache_Click" Visibility="{Binding IsFile, Converter={StaticResource BoolToVis}}">
                                        <MenuItem.Icon>
                                            <TextBlock Text="🔄" VerticalAlignment="Center"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                    <MenuItem Header="Open Folder in Explorer" Click="BtnOpenFolder_Click">
                                        <MenuItem.Icon>
                                            <TextBlock Text="📂" VerticalAlignment="Center"/>
                                        </MenuItem.Icon>
                                    </MenuItem>
                                </ContextMenu>

                                <HierarchicalDataTemplate DataType="{x:Type features:FolderTreeNode}" ItemsSource="{Binding Children}">
                                    <StackPanel Orientation="Horizontal" Margin="2,1" ToolTip="{Binding StatusTooltip}" Background="Transparent" ContextMenu="{StaticResource FolderTreeContextMenu}">
                                        <TextBlock Text="{Binding Icon}" FontSize="9" Margin="0,0,5,0" VerticalAlignment="Center" Foreground="{Binding IconColor}"/>
                                        <TextBlock Text="{Binding DisplayName}" FontSize="9" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                    </StackPanel>
                                </HierarchicalDataTemplate>
                            </TreeView.Resources>
                        </TreeView>
                    </DockPanel>
                </Border>

                <GridSplitter Grid.Column="1" Width="4" HorizontalAlignment="Center" VerticalAlignment="Stretch" Background="{DynamicResource SurfaceHoverBrush}" Cursor="SizeWE"/>

                <Grid Grid.Column="2">
                    <ListBox x:Name="ShapeGallery" 
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled" 
                             ScrollViewer.VerticalScrollBarVisibility="Auto"
                             HorizontalAlignment="Stretch"
                             VerticalAlignment="Stretch"
                             SelectionMode="Extended"
                             SelectionChanged="ShapeGallery_SelectionChanged"
                             ItemContainerStyle="{StaticResource LibraryItemStyle}"
                             MouseDoubleClick="ShapeGallery_MouseDoubleClick"
                             BorderThickness="0" Background="Transparent">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True" ItemWidth="140" ItemHeight="140" Orientation="Horizontal"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="120" Height="120">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Background="{DynamicResource SurfaceLightBrush}" Margin="0,0,0,4">
                                        <Image Source="{Binding ThumbnailSource}" Stretch="Uniform"/>
                                    </Border>
                                    <Border x:Name="HoverOverlay" Grid.Row="0" Background="#44000000" Opacity="0">
                                        <Button Content="Insert" Click="BtnInsertShape_Click" Tag="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="8,4" Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"/>
                                    </Border>
                                    <TextBlock Grid.Row="1" Text="{Binding Title}" FontSize="11" TextTrimming="CharacterEllipsis" Foreground="{DynamicResource TextPrimaryBrush}"/>
                                </Grid>
                                <DataTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="HoverOverlay" Property="Opacity" Value="1"/>
                                    </Trigger>
                                </DataTemplate.Triggers>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Border x:Name="LoadingOverlay" Background="#80FFFFFF" Visibility="Collapsed">
                        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                            <TextBlock x:Name="TxtLoadingDetails" Text="Scanning..." FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                            <ProgressBar IsIndeterminate="True" Width="150" Height="4" Margin="0,10,0,0"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Grid>

            <Grid x:Name="SettingsView" Visibility="Collapsed" HorizontalAlignment="Right" Width="400" Background="{DynamicResource SurfaceBrush}">
                <Grid.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="-2" Opacity="0.3" Direction="180"/>
                </Grid.Effect>
                <Grid.RenderTransform>
                    <TranslateTransform X="400"/>
                </Grid.RenderTransform>
                <ScrollViewer x:Name="SettingsScrollViewer" VerticalScrollBarVisibility="Auto">
                    <StackPanel Margin="20" MaxWidth="500" HorizontalAlignment="Left">
                        <TextBlock Text="Shape Library Settings" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}" Margin="0,0,0,20"/>
                        <TextBlock Text="General Options" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <TextBlock Text="Shape Library Sources" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <ListBox x:Name="LstFolders" Height="120" Background="{DynamicResource SurfaceBrush}" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Button Content="Add Folder..." Click="BtnAddFolder_Click" Padding="10,5" Margin="0,0,10,0"/>
                            <Button Content="Remove" Click="BtnRemoveFolder_Click" Padding="10,5"/>
                        </StackPanel>

                        <TextBlock Text="Shared Folders" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <ListBox x:Name="LstSharedFolders" Height="100" Background="{DynamicResource SurfaceBrush}" Margin="0,0,0,10"/>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <Button Content="Add Shared Folder..." Click="BtnAddSharedFolder_Click" Padding="10,5" Margin="0,0,10,0"/>
                            <Button Content="Remove" Click="BtnRemoveSharedFolder_Click" Padding="10,5"/>
                        </StackPanel>

                        <TextBlock Text="Local Integrations" FontSize="14" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock x:Name="TxtEmpowerStatus" Text="empower: Checking..." FontSize="12" Margin="0,0,0,5"/>
                            <Button x:Name="BtnAddEmpower" Content="Add empower Library" Click="BtnAddEmpower_Click" Padding="10,5" HorizontalAlignment="Left"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,10">
                            <TextBlock x:Name="TxtThinkCellStatus" Text="think-cell: Checking..." FontSize="12" Margin="0,0,0,5"/>
                            <Button x:Name="BtnAddThinkCell" Content="Add think-cell Library" Click="BtnAddThinkCell_Click" Padding="10,5" HorizontalAlignment="Left"/>
                        </StackPanel>
                        <StackPanel Margin="0,0,0,20">
                            <TextBlock x:Name="TxtEfficientStatus" Text="Efficient Elements: Checking..." FontSize="12" Margin="0,0,0,5"/>
                            <Button x:Name="BtnAddEfficient" Content="Add Efficient Elements" Click="BtnAddEfficient_Click" Padding="10,5" HorizontalAlignment="Left"/>
                        </StackPanel>

                        <Button x:Name="BtnGenerateCache" Content="Generate All Thumbnails" Click="BtnGenerateCache_Click" Padding="15,8" Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0"/>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>

        <Border Grid.Row="2" Padding="12,8" Background="{DynamicResource SurfaceBrush}" BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtStatus" Text="Ready" FontSize="11" Foreground="{DynamicResource TextMutedBrush}" Margin="0,0,15,0" VerticalAlignment="Center"/>
                    <CheckBox x:Name="ChkFoldersOnly" Content="Folders only (hide individual files)" Checked="ChkFoldersOnly_Changed" Unchecked="ChkFoldersOnly_Changed" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock x:Name="TxtSelectionCount" Foreground="{DynamicResource TextPrimaryBrush}" VerticalAlignment="Center" Margin="0,0,15,0" Visibility="Collapsed"/>
                    <Button x:Name="BtnInsertSelection" Content="Insert Selection" Click="BtnInsertSelected_Click" Padding="15,6" Background="{DynamicResource AccentBrush}" Foreground="White" BorderThickness="0" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</UserControl>