<Window x:Class="oPenEfficiency.ShortcutConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Configure Shortcuts" Height="340" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Border" x:Key="SettingCard">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    
    <Border Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="25" ShadowDepth="0" Opacity="0.6" Color="Black"/>
        </Border.Effect>
        <Grid Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="SHORTCUT CONFIG" Style="{StaticResource DialogTitleText}"/>
                    <TextBlock Text="Define a custom keyboard shortcut." Margin="0,4,0,0" Style="{StaticResource DialogSubtitleText}"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Grid.ColumnSpan="2" Grid.RowSpan="4" Style="{StaticResource SettingCard}" Margin="0,0,0,20"/>
            <TextBlock Grid.Row="1" Grid.Column="0" Margin="16,16,15,12" Text="Key 1 (e.g. Ctrl)" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
            <Grid Grid.Row="1" Grid.Column="1" Margin="0,16,16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="ComboKey1" Height="32"/>
                <Button x:Name="BtnAssign1" Grid.Column="1" Content="Assign" Click="BtnAssign1_Click" Width="70" Margin="8,0,0,0" Height="32"/>
            </Grid>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Key 2 (e.g. Shift)" VerticalAlignment="Center" Margin="16,0,15,12" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
            <Grid Grid.Row="2" Grid.Column="1" Margin="0,0,16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="ComboKey2" Height="32"/>
                <Button x:Name="BtnAssign2" Grid.Column="1" Content="Assign" Click="BtnAssign2_Click" Width="70" Margin="8,0,0,0" Height="32"/>
            </Grid>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Key 3 (e.g. A)" VerticalAlignment="Center" Margin="16,0,15,12" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
            <Grid Grid.Row="3" Grid.Column="1" Margin="0,0,16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="ComboKey3" Height="32"/>
                <Button x:Name="BtnAssign3" Grid.Column="1" Content="Assign" Click="BtnAssign3_Click" Width="70" Margin="8,0,0,0" Height="32"/>
            </Grid>

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Key 4" VerticalAlignment="Center" Margin="16,0,15,16" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12"/>
            <Grid Grid.Row="4" Grid.Column="1" Margin="0,0,16,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <ComboBox x:Name="ComboKey4" Height="32"/>
                <Button x:Name="BtnAssign4" Grid.Column="1" Content="Assign" Click="BtnAssign4_Click" Width="70" Margin="8,0,0,0" Height="32"/>
            </Grid>

            <StackPanel Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button x:Name="BtnClear" Content="Clear" Width="90" Height="32" Click="BtnClear_Click" Margin="0,0,10,0" Background="Transparent" Foreground="#EF4444" BorderThickness="0" Cursor="Hand" />
                <Button x:Name="BtnSave" Content="Save" Width="90" Height="32" Click="BtnSave_Click" Style="{StaticResource AccentButton}" Cursor="Hand"/>
                <Button x:Name="BtnCancel" Content="Cancel" Style="{StaticResource SecondaryButton}" Width="90" Height="32" Click="BtnCancel_Click" Margin="10,0,0,0" Cursor="Hand"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
