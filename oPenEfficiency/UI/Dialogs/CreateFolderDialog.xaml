<Window x:Class="oPenEfficiency.UI.CreateFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Create New Folder"
        Width="400" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ShowInTaskbar="False"
        ResizeMode="NoResize">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="0"/>
    </WindowChrome.WindowChrome>

    <Border Background="{DynamicResource SurfaceLightBrush}" CornerRadius="12" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="10" Opacity="0.5" Color="Black"/>
        </Border.Effect>

        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Grid.Row="0" Text="Create New Folder" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"
                       Margin="0,0,0,15"/>

            <!-- Name Input -->
            <StackPanel Grid.Row="1" Margin="0,0,0,20">
                <TextBlock Text="Folder Name" FontSize="12" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,8"/>
                <TextBox Style="{StaticResource InlineTextBoxStyle}" x:Name="TxtFolderName" FontSize="13" Padding="10,8"
                         Background="{DynamicResource WindowBackgroundBrush}" Foreground="{DynamicResource TextPrimaryBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                         KeyDown="TxtFolderName_KeyDown"/>
            </StackPanel>

            <!-- Buttons -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button  Content="Cancel" Style="{StaticResource SecondaryButton}" Width="90" Height="32" Margin="0,0,10,0" Cursor="Hand"
                        Click="BtnCancel_Click"/>
                <Button  Content="Create" Style="{StaticResource AccentButton}" Width="90" Height="32" Cursor="Hand"
                        Click="BtnCreate_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
