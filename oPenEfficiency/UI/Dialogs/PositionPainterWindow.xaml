<Window x:Class="oPenEfficiency.UI.PositionPainterWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Position Painter" Width="300" Height="280"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="CenterOwner"
        MouseDown="Window_MouseDown">
        <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Border" x:Key="SettingCard">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>
    <Border CornerRadius="12" Background="{DynamicResource WindowBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <Grid Margin="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid>
                <StackPanel>
                    <TextBlock Text="POSITION PAINTER" Style="{StaticResource DialogTitleText}" FontSize="14"/>
                </StackPanel>
                <Button Style="{StaticResource WindowCloseButton}" Click="Close_Click" HorizontalAlignment="Right" VerticalAlignment="Top"/>
            </Grid>
            <StackPanel Grid.Row="1" Margin="0,15,0,0">
                <TextBlock Text="Select properties to paint:" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                
                <CheckBox x:Name="CbLeft" Content="{Binding LeftText}" IsChecked="True" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                <CheckBox x:Name="CbTop" Content="{Binding TopText}" IsChecked="True" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                <CheckBox x:Name="CbWidth" Content="{Binding WidthText}" IsChecked="True" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,5"/>
                <CheckBox x:Name="CbHeight" Content="{Binding HeightText}" IsChecked="True" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,10"/>
            </StackPanel>
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Learn Again" Click="Learn_Click" Style="{StaticResource OutlineButton}" Margin="0,0,10,0"/>
                <Button Content="Apply" Click="Apply_Click" Style="{StaticResource PrimaryButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>