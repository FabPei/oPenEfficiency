<Window x:Class="oPenEfficiency.UI.ArrangeProWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:oPenEfficiency.UI"
        Title="Arrange Pro" Width="220" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" WindowStartupLocation="Manual"
        MouseDown="Window_MouseDown">
        <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <Style TargetType="Border" x:Key="SettingCard">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>
            
            <local:IndexToBoolConverter x:Key="IndexToBoolConverter"/>
            <local:IndexToOpacityConverter x:Key="IndexToOpacityConverter"/>
            
            <Style x:Key="LabelStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="Margin" Value="0,8,0,2"/>
            </Style>
            
            <Style x:Key="NumericInput" TargetType="TextBox" BasedOn="{StaticResource {x:Type TextBox}}">
                <Setter Property="Padding" Value="4,2"/>
                <Setter Property="FontSize" Value="11"/>
                <Setter Property="HorizontalContentAlignment" Value="Center"/>
            </Style>

            <Style x:Key="ComboBoxItemStyle" TargetType="ComboBoxItem">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                <Setter Property="HorizontalContentAlignment" Value="Left"/>
                <Setter Property="VerticalContentAlignment" Value="Center"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBoxItem">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Padding="{TemplateBinding Padding}" CornerRadius="4" Margin="2,1">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceLightBrush}"/>
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border CornerRadius="12" Background="{DynamicResource WindowBackgroundBrush}" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}" Padding="15">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <StackPanel>
            <Grid Margin="0,0,0,12">
                <TextBlock Text="ARRANGE PRO" Style="{StaticResource DialogTitleText}" VerticalAlignment="Center" />
                <Button Style="{StaticResource WindowCloseButton}" Click="Close_Click" />
            </Grid>

            <TextBlock Text="Shape Type" Style="{StaticResource LabelStyle}" Margin="0,0,0,2"/>
            <ComboBox x:Name="ShapeTypeCombo" SelectedIndex="0" ItemContainerStyle="{StaticResource ComboBoxItemStyle}" SelectionChanged="Combo_SelectionChanged" Height="32">
                <ComboBoxItem Content="Circle"/>
                <ComboBoxItem Content="Triangle"/>
                <ComboBoxItem Content="Square"/>
                <ComboBoxItem Content="Pentagon"/>
                <ComboBoxItem Content="Octagon"/>
                <ComboBoxItem Content="Star"/>
            </ComboBox>

            <TextBlock Text="Instances" Style="{StaticResource LabelStyle}"/>
            <TextBox x:Name="InstancesBox" Text="8" Style="{StaticResource NumericInput}" TextChanged="TextBox_TextChanged" Height="28"/>
            <TextBlock Text="* Only used when 1 object is selected (Duplication)" Foreground="{DynamicResource TextMutedBrush}" FontSize="9" FontStyle="Italic" Margin="0,4,0,0" TextWrapping="Wrap"/>

            <StackPanel Margin="0,4,0,0">
                <TextBlock Text="Radius" Style="{StaticResource LabelStyle}"/>
                <DockPanel>
                    <TextBox x:Name="RadiusBox" Text="{Binding Value, ElementName=RadiusSlider, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0}}" Width="40" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource NumericInput}" Height="24"/>
                    <Slider x:Name="RadiusSlider" Minimum="10" Maximum="500" Value="150" VerticalAlignment="Center" ValueChanged="OnSettingChanged"/>
                </DockPanel>
            </StackPanel>

            <TextBlock Text="Start Angle (Degrees)" Style="{StaticResource LabelStyle}"/>
            <DockPanel>
                <TextBox x:Name="AngleBox" Text="{Binding Value, ElementName=AngleSlider, UpdateSourceTrigger=PropertyChanged, StringFormat={}{0:0}}" Width="40" DockPanel.Dock="Right" Margin="8,0,0,0" Style="{StaticResource NumericInput}" Height="24"/>
                <Slider x:Name="AngleSlider" Minimum="0" Maximum="360" Value="0" VerticalAlignment="Center" ValueChanged="OnSettingChanged"/>
            </DockPanel>

            <TextBlock Text="Center Point" Style="{StaticResource LabelStyle}"/>
            <StackPanel Orientation="Horizontal">
                <RadioButton x:Name="CenterSelection" Content="Selection" IsChecked="True" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" Margin="0,0,15,0" Checked="Radio_Checked"/>
                <RadioButton x:Name="CenterSlide" Content="Slide" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" Checked="Radio_Checked"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" Margin="0,15,0,0">
                <CheckBox x:Name="RotateToCenterCheck" Content="Rotate to Center" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" Margin="0,0,15,0" IsChecked="False" Checked="Check_Changed" Unchecked="Check_Changed"/>
                <CheckBox x:Name="FillShapeCheck" Content="Fill Shape" Foreground="{DynamicResource TextPrimaryBrush}" FontSize="11" IsChecked="True" Checked="Check_Changed" Unchecked="Check_Changed"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
