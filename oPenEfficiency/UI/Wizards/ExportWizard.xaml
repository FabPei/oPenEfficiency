<Window x:Class="oPenEfficiency.UI.ExportWizard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save or Send..." Height="750" Width="800"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" MouseDown="Window_MouseDown"
        FontFamily="Segoe UI Variable Small, Segoe UI">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/oPenEfficiency;component/UI/Styles/Controls.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            
                        <Style x:Key="FormatToggleStyle" TargetType="RadioButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderLightBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="10"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
                                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Background" Value="#0AFFFFFF"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                    <Setter TargetName="Bd" Property="Background" Value="#100078D4"/>
                                    <Setter TargetName="Bd" Property="BorderThickness" Value="2"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="Border" x:Key="SettingCard">
                <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="CornerRadius" Value="4"/>
                <Setter Property="Padding" Value="12"/>
                <Setter Property="Margin" Value="0,0,0,15"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Border Background="{DynamicResource WindowBackgroundBrush}" CornerRadius="12" BorderThickness="1" BorderBrush="{DynamicResource BorderBrush}">
        <Border.Effect>
            <DropShadowEffect BlurRadius="30" ShadowDepth="10" Opacity="0.5" Color="Black"/>
        </Border.Effect>
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <DockPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="Save or Send..." FontSize="20" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}"/>
                <Button x:Name="BtnClose" Style="{StaticResource WindowCloseButton}" Click="BtnCancel_Click" HorizontalAlignment="Right" />
            </DockPanel>

            <!-- Content Area -->
            <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0,1,0,0" BorderBrush="{DynamicResource BorderBrush}">
                <TabControl.Resources>
                    <Style TargetType="TabItem">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
                        <Setter Property="Padding" Value="16,10"/>
                        <Setter Property="FontSize" Value="14"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="TabItem">
                                    <Border Background="{TemplateBinding Background}" BorderThickness="0">
                                        <ContentPresenter ContentSource="Header" Margin="{TemplateBinding Padding}"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TabControl.Resources>
                
                <!-- Tab: Options -->
                <TabItem Header="Options">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Grid Margin="10,10,20,20">
                            <StackPanel>
                                <!-- Two Columns Section -->
                                <Grid Margin="0,0,0,20">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="30"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <!-- Left: Format -->
                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Format" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <Border Style="{StaticResource SettingCard}">
                                            <StackPanel>
                                                <RadioButton x:Name="FormatPPTX" IsChecked="True" GroupName="Format" Style="{StaticResource FormatToggleStyle}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Path Data="M6,2H14L20,8V20A2,2 0 0,1 18,22H6A2,2 0 0,1 4,20V4A2,2 0 0,1 6,2M13,3.5V9H18.5L13,3.5M16,11H8V13H16V11M16,15H8V17H16V15Z" Fill="#D32F2F" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform"/>
                                                        <TextBlock Text="PPTX"/>
                                                    </StackPanel>
                                                </RadioButton>
                                                <RadioButton x:Name="FormatPDF" GroupName="Format" Style="{StaticResource FormatToggleStyle}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Path Data="M19,2H5C3.89,2 3,2.89 3,4V20A2,2 0 0,0 5,22H19A2,2 0 0,0 21,20V4C21,2.89 20.11,2 19,2M12,18L7,13H10V9H14V13H17L12,18Z" Fill="#F44336" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform"/>
                                                        <TextBlock Text="PDF"/>
                                                    </StackPanel>
                                                </RadioButton>
                                                <RadioButton x:Name="FormatBoth" GroupName="Format" Style="{StaticResource FormatToggleStyle}">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Path Data="M15,14H19V17H15V14M11,14H13V17H11V14M7,14H9V17H7V14M21,11H3V20H21V11M21,1H3V10H21V1M19,4H15V7H19V4M13,4H11V7H13V4M9,4H7V7H9V4Z" Fill="#FF9800" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform"/>
                                                        <TextBlock Text="PPTX + PDF"/>
                                                    </StackPanel>
                                                </RadioButton>
                                                <RadioButton x:Name="FormatProtected" GroupName="Format" Style="{StaticResource FormatToggleStyle}" Margin="0,0,0,10">
                                                    <StackPanel Orientation="Horizontal">
                                                        <Path Data="M12,17A2,2 0 0,0 14,15C14,13.89 13.1,13 12,13A2,2 0 0,0 10,15C10,16.11 10.9,17 12,17M18,8H17V6A5,5 0 0,0 12,1A5,5 0 0,0 7,6V8H6A2,2 0 0,0 4,10V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V10A2,2 0 0,0 18,8M9,6A3,3 0 0,1 12,3A3,3 0 0,1 15,6V8H9V6Z" Fill="#607D8B" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform"/>
                                                        <TextBlock Text="Protected PDF"/>
                                                    </StackPanel>
                                                </RadioButton>
                                                
                                                <StackPanel Margin="24,10,0,0" x:Name="PdfOptionsPanel">
                                                    <CheckBox x:Name="ChkPdfA" Content="PDF/A Compliance (ISO 19005-1)" FontSize="11" Margin="0,0,0,5"/>
                                                    <CheckBox x:Name="ChkPdfFrame" Content="Add Frame around Slides" FontSize="11"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>

                                    <!-- Right: Scope -->
                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Scope (Slides)" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,12"/>
                                        <Border Style="{StaticResource SettingCard}">
                                            <StackPanel>
                                                <RadioButton x:Name="ScopeAll" IsChecked="True" GroupName="Scope" Margin="0,0,0,10">
                                                    <TextBlock x:Name="TxtScopeAll" Text="Entire Presentation (0)"/>
                                                </RadioButton>
                                                <RadioButton x:Name="ScopeSelected" GroupName="Scope" Margin="0,0,0,10">
                                                    <TextBlock x:Name="TxtScopeSelected" Text="Selected Slides (0)"/>
                                                </RadioButton>
                                                <CheckBox x:Name="ChkOnlyVisible" Content="Only Visible Slides" Margin="0,5,0,10" IsEnabled="False"/>
                                                
                                                <StackPanel Margin="0,10,0,0">
                                                    <TextBlock Text="Date" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                                                    <DatePicker x:Name="PickerDate" Height="28"/>
                                                    <CheckBox x:Name="ChkAutoDate" Content="Auto" IsChecked="True" Margin="0,10,0,0"/>

                                                    <TextBlock Text="Version" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                                                    <TextBox x:Name="TxtVersion" Text="1" Width="50" HorizontalAlignment="Left" Padding="5,2" Margin="0,0,0,10"/>
                                                    <CheckBox x:Name="ChkAutoVersion" Content="Auto" IsChecked="True"/>
                                                </StackPanel>
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                </Grid>

                                <!-- Filename Section -->
                                <Border Style="{StaticResource SettingCard}">
                                    <StackPanel>
                                        <!-- Metadata Row -->
                                        <Grid Margin="0,0,0,15">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="2*"/>
                                                <ColumnDefinition Width="20"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock Text="Theme / Topic" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                                                <TextBox x:Name="TxtTheme" Text="Extract" Padding="5,2"/>
                                            </StackPanel>
                                            <StackPanel Grid.Column="2">
                                                <TextBlock Text="Initials" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                                                <TextBox x:Name="TxtInitials" Text="PF" Padding="5,2"/>
                                            </StackPanel>
                                        </Grid>
                                        
                                        <TextBlock Text="Filename" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                                        <TextBox x:Name="TxtFinalFilename" FontSize="14" FontWeight="SemiBold" VerticalContentAlignment="Center" Height="40"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
                
                <!-- Tab: Properties -->
                <TabItem Header="Properties">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Grid Margin="10,10,20,20">
                            <StackPanel>
                                <TextBlock Text="Edit Document Properties" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                                
                                <!-- Group 1: Standard PDF & PPTX -->
                                <Expander Header="Standard Properties (PDF &amp; PPTX)" IsExpanded="True" Foreground="{DynamicResource TextPrimaryBrush}" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,0,15" Padding="10">
                                    <StackPanel Margin="0,10,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="10"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="10"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="10"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Title" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropTitle" Grid.Row="0" Grid.Column="1"/>

                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Author" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropAuthor" Grid.Row="2" Grid.Column="1"/>

                                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Subject" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropSubject" Grid.Row="4" Grid.Column="1"/>

                                            <TextBlock Grid.Row="6" Grid.Column="0" Text="Keywords" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropKeywords" Grid.Row="6" Grid.Column="1" ToolTip="Separate tags with semicolons"/>
                                        </Grid>
                                    </StackPanel>
                                </Expander>

                                <!-- Group 2: Extended PPTX / XMP -->
                                <Expander Header="Extended Metadata (PPTX &amp; XMP)" IsExpanded="False" Foreground="{DynamicResource TextPrimaryBrush}" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,0,15" Padding="10">
                                    <StackPanel Margin="0,10,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="10"/>
                                                <RowDefinition Height="Auto"/>
                                                <RowDefinition Height="10"/>
                                                <RowDefinition Height="Auto"/>
                                            </Grid.RowDefinitions>

                                            <!-- Row 1 -->
                                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Company" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropCompany" Grid.Row="0" Grid.Column="1"/>

                                            <TextBlock Grid.Row="0" Grid.Column="3" Text="Manager" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropManager" Grid.Row="0" Grid.Column="4"/>

                                            <!-- Row 2 -->
                                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Category" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropCategory" Grid.Row="2" Grid.Column="1"/>

                                            <TextBlock Grid.Row="2" Grid.Column="3" Text="Status" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropStatus" Grid.Row="2" Grid.Column="4"/>

                                            <!-- Row 3: Hyperlink Base -->
                                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Hyperlink Base" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropHyperlinkBase" Grid.Row="4" Grid.Column="1" Grid.ColumnSpan="4"/>
                                        </Grid>
                                    </StackPanel>
                                </Expander>

                                <!-- Group 3: Content / Comments -->
                                <Expander Header="Content &amp; Notes" IsExpanded="False" Foreground="{DynamicResource TextPrimaryBrush}" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" Margin="0,0,0,15" Padding="10">
                                    <StackPanel Margin="0,10,0,0">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="Comments" VerticalAlignment="Top" Margin="0,4,0,0" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                            <TextBox x:Name="TxtPropComments" Grid.Column="1" Height="60" TextWrapping="Wrap" AcceptsReturn="True" VerticalContentAlignment="Top" Padding="8,8"/>
                                        </Grid>
                                    </StackPanel>
                                </Expander>
                                
                                <CheckBox x:Name="ChkApplyPropsOnExport" Content="Save properties permanently in original file" IsChecked="True" Margin="0,0,0,20"/>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
                
                <!-- Tab: Cleanup -->
                <TabItem Header="Cleanup">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <Grid Margin="10,10,20,20">
                            <StackPanel>
                                <TextBlock Text="Validate &amp; Cleanup Presentation" FontSize="16" FontWeight="SemiBold" Foreground="{DynamicResource TextPrimaryBrush}" Margin="0,0,0,15"/>
                                <Border Style="{StaticResource SettingCard}">
                                    <StackPanel>
                                        <CheckBox x:Name="ChkCleanComments" Content="Remove Comments" IsChecked="False" Margin="0,0,0,10"/>
                                        <CheckBox x:Name="ChkCleanHiddenSlides" Content="Remove Hidden Slides" IsChecked="False" Margin="0,0,0,10"/>
                                        <CheckBox x:Name="ChkCleanNotes" Content="Remove Speaker Notes" IsChecked="False" Margin="0,0,0,10"/>
                                        <CheckBox x:Name="ChkCleanDeveloperNotes" Content="Remove Developer Notes (oPE Scratchpad)" IsChecked="False" Margin="0,0,0,10"/>
                                        <CheckBox x:Name="ChkCleanMetadata" Content="Remove Personal Info / Metadata (Author, etc.)" IsChecked="False" Margin="0,0,0,10"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </ScrollViewer>
                </TabItem>
            </TabControl>

            <!-- Footer -->
            <DockPanel Grid.Row="2" Margin="0,20,0,0" LastChildFill="False">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="Destination:" VerticalAlignment="Center" Margin="20,0,15,0" FontSize="13" Foreground="{DynamicResource TextSecondaryBrush}"/>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <RadioButton x:Name="DestFile" Content="File (Disk)" IsChecked="True" Margin="0,0,15,0"/>
                        <RadioButton x:Name="DestClipboard" Content="Clipboard" Margin="0,0,15,0"/>
                        <RadioButton x:Name="DestMail" Content="E-Mail"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button  Content="Cancel" Style="{StaticResource SecondaryButton}" Width="100" Height="36" Click="BtnCancel_Click" Margin="0,0,10,0">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                    <Button x:Name="BtnOK" Content="OK" Style="{StaticResource AccentButton}" Width="120" Height="36" Click="BtnOK_Click">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="bdr" Background="{TemplateBinding Background}" CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="bdr" Property="Background" Value="{DynamicResource AccentHoverBrush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</Window>
